import React from 'react';

const ContactsList = () => (
  <header className="header-top navbar navbar-dark bg-dark">
    <h1 className="header-top-heading container-fluid">Contacts</h1>
  </header>
);

export default ContactsList;